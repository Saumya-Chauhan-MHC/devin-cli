# Example environment configuration for Devin CLI

# Default GitHub repository to interact with (you can override via --repo)
DEFAULT_REPO=your-org/your-repo

# Optional GitHub token (needed only for private repositories)
# Generate via GitHub -> Settings -> Developer Settings -> Personal Access Tokens
GITHUB_TOKEN=ghp_your_github_token_here

# Devin API key (from Devin -> Settings -> Devin's API -> View Key)
DEVIN_API_KEY=apk_user_your_devin_api_key_here

# Let Devin’s GitHub App create branches and PRs automatically
DEVIN_USE_GH_APP=true
